# 家事管理LINEボット セットアップガイド（2026年4月版）

約30〜40分で完成します。完全無料で運用できます。

> ⚠️ **2024年9月の仕様変更**：LINE DevelopersコンソールからMessaging APIチャネルを直接作成することはできなくなりました。まずLINE公式アカウントを作り、そこからMessaging APIを有効化する流れになります。

---

## 📦 必要なもの

- Googleアカウント（スプレッドシートとGAS用）
- LINEアカウント（夫婦2人分）
- スマホまたはPC

---

## 全体の流れ

```
1. LINE公式アカウントを作成 (LINE Official Account Manager)
   ↓
2. Messaging APIを有効化 → プロバイダーが作られチャネル自動生成
   ↓
3. LINE Developersコンソールで設定 (アクセストークン発行など)
   ↓
4. Googleスプレッドシート + GASでボット実装
   ↓
5. Webhookを繋いで完成 🎉
```

---

## Step 1: LINE公式アカウントを作る（10分）

### 1-1. LINE公式アカウント開設ページへ

https://www.linebiz.com/jp/entry/ にアクセス

「LINE公式アカウントを開設する（無料）」をクリック

### 1-2. ログイン

- 既存のLINEアカウントでログイン、または
- ビジネスアカウント（メールアドレス）を新規作成

### 1-3. アカウント情報を入力

- **アカウント名**: `家事ボット` など（あとで変更可）
- **メールアドレス**: 自分のメール
- **業種**: `個人` → `個人（その他）`

→ 入力完了で **LINE公式アカウント作成完了** ✅

---

## Step 2: Messaging APIを有効化する（5分）

### 2-1. LINE Official Account Managerにログイン

https://manager.line.biz/ にアクセスして、作ったアカウントを開く

### 2-2. Messaging APIを有効化

1. 右上の **「設定」** をクリック
2. 左メニューの **「Messaging API」** を選択
3. **「Messaging APIを利用する」** をクリック
4. **プロバイダーを新規作成**（例: `ひろしの家`）
   > ⚠️ **注意**: プロバイダーは後から変更できません。個人利用なら新規作成でOK
5. 「同意する」をクリック
6. プライバシーポリシー・利用規約のURL（任意、空欄でOK）→「OK」

→ **Messaging APIチャネルが自動生成されます** ✅

### 2-3. 応答設定を変更（重要）

引き続きLINE Official Account Managerで：

1. 左メニュー「応答設定」を開く
2. **「応答メッセージ」を OFF**（自動定型返信を切る）
3. **「Webhook」を ON**
4. 「あいさつメッセージ」はON/OFFどちらでもOK

---

## Step 3: LINE Developersでアクセストークンを発行（5分）

### 3-1. LINE Developersコンソールにアクセス

https://developers.line.biz/console/ を開く

→ 同じLINEアカウントでログインすると、Step 2で作ったプロバイダーとチャネルが見えるはず

### 3-2. チャネルを開く

プロバイダー → 作成されたチャネル（家事ボットの名前）をクリック

### 3-3. アクセストークンを発行

- **「Messaging API設定」タブ** を開く
- 一番下までスクロール
- **「チャネルアクセストークン（長期）」** の「発行」ボタンをクリック
- 発行されたトークンを **メモ帳にコピー** 📋（後で使います）

### 3-4. QRコードで友だち追加

「Messaging API設定」タブの上部にあるQRコードを、夫婦両方のLINEで読み取って友だち追加

---

## Step 4: Googleスプレッドシートを作る（2分）

1. https://sheets.new で新規作成
2. 名前を `家事ボットDB` などに変更
3. URLから **スプレッドシートID** をコピー 📋

```
https://docs.google.com/spreadsheets/d/1AbCdEfGhIjKlMnOpQrStUv/edit
                                       ↑↑↑↑↑↑ここをコピー↑↑↑↑↑↑
```

---

## Step 5: Google Apps Scriptにコードを入れる（10分）

### 5-1. GASプロジェクトを開く

スプレッドシートのメニュー：**拡張機能 → Apps Script**

新しいタブでエディタが開くので、`コード.gs` の中身をすべて削除

### 5-2. コードを貼り付け

`Code.gs` の中身を全部コピペして保存（Ctrl+S / Cmd+S）

### 5-3. スクリプトプロパティを設定

左メニューの **⚙️ プロジェクトの設定** → 一番下「スクリプトプロパティ」

**「スクリプトプロパティを追加」** で以下の2つを追加：

| プロパティ名 | 値 |
|----|----|
| `CHANNEL_ACCESS_TOKEN` | Step 3-3で発行したトークン |
| `SPREADSHEET_ID` | Step 4でコピーしたID |

→ 「スクリプトプロパティを保存」

### 5-4. 初期セットアップ実行

エディタに戻り、上部の関数選択で `setup` を選択 → **▶ 実行**

初回は権限の承認ダイアログが出ます：
1. 「権限を確認」をクリック
2. Googleアカウントを選択
3. 「詳細」→「(プロジェクト名) に移動（安全ではないページ）」
4. 「許可」

実行後、スプレッドシートに `DailyLog` と `CustomTodos` のタブができていればOK ✅

---

## Step 6: Webhookを設定してLINEと繋ぐ（5分）

### 6-1. GASをウェブアプリとしてデプロイ

GASエディタ右上の **「デプロイ」→「新しいデプロイ」**

歯車アイコン → **「ウェブアプリ」** を選択

設定：
- 説明: `v1`（任意）
- 実行ユーザー: **自分**
- アクセスできるユーザー: **全員**

→ **「デプロイ」** をクリック

### 6-2. URLをコピー

**ウェブアプリのURL** をコピー 📋

```
https://script.google.com/macros/s/AKfycbx...../exec
```

### 6-3. LINE DevelopersにWebhookを登録

LINE Developersコンソールに戻り、チャネルの **「Messaging API設定」タブ**：

1. 「Webhook URL」の **「編集」** をクリック
2. コピーしたURLを貼り付け → **「更新」**
3. **「Webhookの利用」を ON**（既にONなら何もしなくてOK）
4. **「検証」ボタン** をクリック → `成功` が出ればOK ✅

---

## Step 7: アクセス制限を設定する（重要・5分）🔒

デフォルトのままだとボットを友だち追加した**誰でも**使えてしまいます。夫婦のuserIdだけ許可するように設定します。

### 7-1. 登録モードをONにする

GASエディタで `Code.gs` の20行目を変更：

```javascript
const REGISTRATION_MODE = true; // false → true に変更
```

**Ctrl+S / Cmd+S で保存** → 再度デプロイ：
- 「デプロイ」→「デプロイを管理」→ 鉛筆アイコン編集
- バージョン: **新バージョン** を選択 → 「デプロイ」

### 7-2. 夫婦それぞれでuserIdを取得

夫婦それぞれが、LINEでボットに `テスト` などと送信

→ ボットから返信：
```
🔧 登録モード
あなたのuserId:
U1234567890abcdef...
```

この **`U〜` で始まる33文字** をそれぞれメモ 📋

### 7-3. ALLOWED_USER_IDSを登録

GASの **⚙️ プロジェクトの設定 → スクリプトプロパティ**：

| プロパティ名 | 値 |
|----|----|
| `ALLOWED_USER_IDS` | `U夫のID,U妻のID` （カンマ区切り・スペース不要） |

### 7-4. 登録モードをOFFに戻す

`Code.gs` の21行目を元に戻す：

```javascript
const REGISTRATION_MODE = false; // trueに戻さないこと！
```

保存 → 再度デプロイ（7-1と同じ手順）

### 7-5. 動作確認

- 夫婦のアカウントから `今日` と送信 → 正常に返信が来る ✅
- 他の人のアカウントから送信 → **完全無視される**（返信なし）✅

> ⚠️ 万が一第三者がボットのQRコードを手に入れて友だち追加しても、userIdがホワイトリストに無いので一切反応しません

---

## Step 8: 使ってみる 🎉

LINEでボットに話しかけてみましょう！

```
おはよう
```
→ 今日のタスク一覧が返ってくる

```
完了 朝食
```
→ 完了が記録される

```
追加 病院予約
```
→ 永続タスクが追加される

---

## 🔧 使えるコマンド一覧

| コマンド | 動作 |
|---|---|
| `今日` / `おはよう` | 今日のタスクと進捗表示 |
| `完了 〇〇` | タスクを完了にする |
| `取消 〇〇` | 完了を取り消し |
| `追加 〇〇` | 永続ToDo追加 |
| `リスト` | 追加ToDo一覧 |
| `削除 〇〇` | 追加ToDo削除 |
| `ヘルプ` | コマンド一覧 |

---

## 💡 発展: 夫婦グループで使う + 毎朝自動送信

### 夫婦グループで使う方法

1. LINEで **夫婦2人 + ボット** のグループを作成
2. グループに**ボットを招待**（これで3人グループ）
3. グループで何か送ると、GASの実行ログに `groupId` が記録される
   - デバッグ用: `handleMessage` 冒頭に `console.log(event.source)` を一時的に追加
   - 「実行数」メニューでログ確認
4. その `groupId` をスクリプトプロパティに `GROUP_ID` として追加

### 毎朝7時に自動通知

GASエディタ左メニューの **⏰ トリガー** → 「トリガーを追加」

- 関数: `morningPush`
- イベントのソース: **時間主導型**
- 時間ベースのタイマー: **日タイマー**
- 時刻: **午前7時〜8時**

→ 保存で毎朝グループに今日のタスクが自動投稿 🌅

---

## ❓ トラブルシューティング

**「Messaging APIを利用する」ボタンが見つからない**
→ LINE Official Account Manager（manager.line.biz）にログインできているか確認。LINE Developersコンソールと間違えやすい

**Webhook検証で失敗する**
→ GASで「新しいデプロイ」をもう一度行い、新しいURLをLINEに再登録（既存デプロイを更新するとURLが変わらない場合がある）

**「権限がありません」エラー**
→ GASで `setup` 関数を手動実行して、Googleアカウントの権限承認

**ボットが返信しない**
→ 以下を順に確認:
- LINE Official Account Managerで「応答メッセージ」がOFFか
- LINE Official Account Managerで「Webhook」がONか
- LINE DevelopersでもWebhookがONか（両方ONが必要）
- GASのデプロイアクセス権が「全員」になっているか

**タスク名がずれて記録される**
→ 「完了 朝食」のように**半角スペース1個**で区切る。絵文字や記号もタスク名と完全一致する必要あり

**無料枠を超えないか心配**
→ LINE Messaging APIの無料枠は**月200通のプッシュメッセージ**。応答（reply）は無制限。自動送信を毎朝1通 × 2人分でも月60通程度なので安心

---

## 🚀 今後の拡張アイデア

- 週次レポート（夫婦どちらが何件やったか）
- 夜までに終わってないタスクのリマインダー
- 買い物リスト機能
- リッチメニュー（ボタン式UI）
- Googleカレンダー連携
- 子供の予定管理（予防接種など）

拡張アイデアについては [README.md](README.md) も参照してください。
